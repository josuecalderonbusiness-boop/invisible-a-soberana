{
  "name": "soberana-firestore-rules-tests",
  "private": true,
  "type": "module",
  "description": "Pruebas de firestore.rules contra el Firestore Emulator (proyecto demo, nunca produccion).",
  "scripts": {
    "test": "cd .. && firebase emulators:exec --only firestore --project demo-soberana-rules \"node --test --test-concurrency=1 rules-tests/firestore-rules.test.js rules-tests/firestore-rules-compat.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.19.0",
    "firebase-admin": "^14.4.0",
    "firebase9": "npm:firebase@^9.23.0"
  }
}
